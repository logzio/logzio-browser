/* Service settings */
export const DEFAULT_SERVICE_NAME = '';
export const DEFAULT_SERVICE_VERSION = '';

/* Session settings */
export const DEFAULT_SESSION_MAX_DURATION_MS = 4 * 60 * 60 * 1000; // 4 hours
export const DEFAULT_SESSION_TIMEOUT_MS = 15 * 60 * 1000; // 15 minutes

/* Data settings */
export const DEFAULT_ENABLE_STATE = true;
export const DEFAULT_DISABLE_STATE = false;
export const DEFAULT_SAMPLE_RATE = 100; // 100%

/* Frustration signals settings */
export const DEFAULT_RAGE_CLICK_COUNT = 3; // 3 clicks within `rage click interval`
export const DEFAULT_RAGE_CLICK_INTERVAL_MS = 1000; // 1 second
export const DEFAULT_HEAVY_LOAD_THRESHOLD_MS = 2000; // 2 seconds

/* Log settings */
export const DEFAULT_LOG_LEVEL = 'info';
